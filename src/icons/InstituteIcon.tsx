import React from "react";

const InstituteIcon = ({ className }: { className: string }) => {
  return (
    <img
        className={className}
        alt="ícone do instituto besouro"
        src={"/assets/iconeInstituto.png"}
    />
  );
};

export default InstituteIcon;
